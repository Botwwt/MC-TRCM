# Audits

Audit outputs for split integrity, leakage checks, baseline results, and registry consistency.

If an audit conflicts with regenerated experiment outputs, rerun the corresponding preprocessing or result script before updating paper claims.
