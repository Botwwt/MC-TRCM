# Protocol Configurations

Protocol files document validation-locked comparison rules.

`STRICT_ALIGNED_WITHIN_DATASET.json` is the paper-facing protocol for target-dataset evaluation. It keeps preprocessing, baseline selection, MC-TRCM selection, calibration, and test reporting separated.
