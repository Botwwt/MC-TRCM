# Training Configurations

Training JSON files define optimizer, learning rate, batch size, early stopping, sampling, calibration, and loss-weighting settings.

`mctrcm_v2_family_a.json` is the compact default used by `train_mctrcm_v2.py`. `default_train.json` is shared by baseline-style runners.
