# Split Configurations

Split configuration files define participant-level train/validation/test policies.

The paper workflow requires that all anchors from the same participant stay in exactly one split. Validation is used for model selection and calibration; test is reserved for final reporting.
